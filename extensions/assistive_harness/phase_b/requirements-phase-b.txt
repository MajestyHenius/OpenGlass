# OpenGlass — extensions/assistive_harness 依赖（②③④ 链路：语音控制 + CV 漏斗）
#
# 装在**和 MiniCPM-o-Demo 同一个 conda 环境**里：panel 起子进程时继承启动它的环境，
# 而 worker/gateway 也在那个环境跑。不要另建环境。
#
#   conda activate <你跑 MiniCPM-o-Demo 的环境>
#   pip install -r requirements-phase-b.txt

# ── 基础 ──
aiohttp>=3.13          # gateway/harness 的 WebSocket 客户端 + bridge_ui 服务
numpy>=1.24            # 音频/图像数组
Pillow>=10             # --rotate 转正（PIL）
sounddevice>=0.5       # PC 扬声器播放（AI 语音、reject 提示音）
requests>=2.31         # ESP32 HTTP 控制（分辨率/对焦）
PyYAML>=6.0            # harness 的 skills 配置

# ── 8021 harness（语音控制腿，②③④ 需要）──
fastapi>=0.110
uvicorn>=0.27
funasr>=1.3            # 流式 ASR。模型另外下载，路径写进 runtime.local.json
cryptography>=42       # panel 首次启动自签证书（没有它会退回调 openssl）

# ── CV 漏斗（③④ 需要）──
opencv-python>=4.8     # 清晰度/光流/亮度判据
#
# ★ 方向分类器（orient_flipped / orient_sideways 判据）——
#   装不上或版本不对时会**静默回退到早期的简易判据**，表现是画面明明是正的
#   却一直报"画面好像反了"。启动日志里会有：
#     [预热] !! PaddleOCR 方向分类器加载失败，已回退到轻量 CV 判据 ...
#   看到这行就说明下面两个没装好，别忽略。
#
#   实测可用组合（CPU 版即可，模型只有 6.75MB）：
paddlepaddle>=3.0      # GPU 机器可换 paddlepaddle-gpu，但 CPU 版足够
paddleocr>=3.0         # 只用其中的 DocImgOrientationClassification
