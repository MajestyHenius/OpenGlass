# Local model directory

Put optional local inference weights here, for example `yolo26n.onnx`.
Weights and accelerator artifacts are ignored by Git; this directory contains
only documentation. ModelScope ASR models normally live in the user's external
cache (`%USERPROFILE%\.cache\modelscope\hub`) and do not need to be copied here.
